{
    "version": 2,
    "builds": [
      {
        "src": "src/main.ts",
        "use": "@vercel/node",
        "config": { "tsConfigFile": "tsconfig.build.json" }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dist": "/"
      }
    ]
  }